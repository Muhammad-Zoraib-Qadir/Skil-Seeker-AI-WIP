import React from "react";

const Navbar = () => {
    return (
        <nav style={{ background: "#333", color: "white", padding: "10px" }}>
            <h2>AI Skill Finder</h2>
        </nav>
    );
};

export default Navbar;
